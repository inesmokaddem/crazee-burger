import React from 'react'
import styled from 'styled-components';

export default function MenuCard() {
  return (
    <MenuCardStyled>MenuCard</MenuCardStyled>
  )
}

const MenuCardStyled = styled.div`
  background-color: green;
`;
